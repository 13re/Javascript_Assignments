// Function Parameters

/* 

To specify the location of the code every time we call the function, 
we must specify parameters in function definition + function call. 

To tell the function that we're passing parameters, 
we add parameter names like shapeX and shapeY inside the parenthesis. 
Now the function will use the parameters we specify when calling the new function. 
*/